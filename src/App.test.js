import { render, screen } from '@testing-library/react';

test('renders learn react link', () => {
  render();
  // const linkElement = screen.getByText(/Compra efetuada com sucesso!/i);
  // expect(linkElement).toBeInTheDocument();
  screen.debug();
});